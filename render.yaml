services:
  - type: web
    name: cegm-music-backend
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 20
      - key: YOUTUBE_API_KEY
        sync: false
      - key: PORT
        value: 3001
